services:
  spark:
    build: ./spark
    container_name: qt_spark
    ports:
      - "8888:8888"
    volumes:
      - ../qt_data:/home/jovyan/work # map the scripts root dir to the container as a volume to persist scripts changes
    environment:
      PYSPARK_SUBMIT_ARGS: "--repositories https://packages.databricks.com/repo/ 
                            --packages io.delta:delta-core_2.12:2.4.0,\
                                       org.apache.hadoop:hadoop-aws:3.3.2,\
                                       org.postgresql:postgresql:42.7.3 
                            pyspark-shell"
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_HOST: ${POSTGRES_HOST}
      POSTGRES_PORT: ${POSTGRES_PORT}
      ALPHA_VANTAGE_API_KEY: ${ALPHA_VANTAGE_API_KEY} # apikey to extract stock data

  db:
    image: postgres:16
    container_name: qt_postgres
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"
    volumes:
      - ./postgres/data:/var/lib/postgresql/data

  # redis:
  #   image: redis:7
  #   container_name: qt_redis
  #   ports:
  #     - "6379:6379"

  # airflow-webserver:
  #   image: apache/airflow:2.9.1
  #   container_name: qt_airflow_web
  #   restart: always
  #   depends_on:
  #     - db
  #     - redis
  #   environment:
  #     AIRFLOW__CORE__EXECUTOR: CeleryExecutor
  #     AIRFLOW__CORE__SQL_ALCHEMY_CONN: postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
  #     AIRFLOW__CELERY__BROKER_URL: redis://redis:6379/0
  #     AIRFLOW__CELERY__RESULT_BACKEND: db+postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
  #     AIRFLOW__CORE__FERNET_KEY: ${AIRFLOW_FERNET_KEY}
  #     AIRFLOW__CORE__LOAD_EXAMPLES: 'false'
  #     AIRFLOW__WEBSERVER__SECRET_KEY: ${AIRFLOW_SECRET_KEY}
  #     _AIRFLOW_WWW_USER_USERNAME: admin
  #     _AIRFLOW_WWW_USER_PASSWORD: admin
  #   ports:
  #     - "8080:8080"
  #   volumes:
  #     - ./airflow/dags:/opt/airflow/dags
  #     - ./airflow/logs:/opt/airflow/logs
  #     - ./airflow/plugins:/opt/airflow/plugins
  #   command: >
  #     bash -c "airflow db upgrade &&
  #              airflow users create --username admin --password admin --firstname Admin --lastname User --role Admin --email admin@example.com || true &&
  #              airflow webserver"

  # airflow-scheduler:
  #   image: apache/airflow:2.9.1
  #   container_name: qt_airflow_scheduler
  #   restart: always
  #   depends_on:
  #     - airflow-webserver
  #   environment:
  #     AIRFLOW__CORE__EXECUTOR: CeleryExecutor
  #     AIRFLOW__CORE__SQL_ALCHEMY_CONN: postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
  #     AIRFLOW__CELERY__BROKER_URL: redis://redis:6379/0
  #     AIRFLOW__CELERY__RESULT_BACKEND: db+postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
  #     AIRFLOW__CORE__FERNET_KEY: ${AIRFLOW_FERNET_KEY}
  #     AIRFLOW__CORE__LOAD_EXAMPLES: 'false'
  #   volumes:
  #     - ./airflow/dags:/opt/airflow/dags
  #     - ./airflow/logs:/opt/airflow/logs
  #     - ./airflow/plugins:/opt/airflow/plugins
  #   command: airflow scheduler

  # airflow-worker:
  #   image: apache/airflow:2.9.1
  #   container_name: qt_airflow_worker
  #   restart: always
  #   depends_on:
  #     - airflow-webserver
  #   environment:
  #     AIRFLOW__CORE__EXECUTOR: CeleryExecutor
  #     AIRFLOW__CORE__SQL_ALCHEMY_CONN: postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
  #     AIRFLOW__CELERY__BROKER_URL: redis://redis:6379/0
  #     AIRFLOW__CELERY__RESULT_BACKEND: db+postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
  #     AIRFLOW__CORE__FERNET_KEY: ${AIRFLOW_FERNET_KEY}
  #     AIRFLOW__CORE__LOAD_EXAMPLES: 'false'
  #   volumes:
  #     - ./airflow/dags:/opt/airflow/dags
  #     - ./airflow/logs:/opt/airflow/logs
  #     - ./airflow/plugins:/opt/airflow/plugins
  #   command: airflow celery worker

  # airflow-flower:
  #   image: apache/airflow:2.9.1
  #   container_name: qt_airflow_flower
  #   restart: always
  #   depends_on:
  #     - airflow-webserver
  #   environment:
  #     AIRFLOW__CORE__EXECUTOR: CeleryExecutor
  #     AIRFLOW__CORE__SQL_ALCHEMY_CONN: postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
  #     AIRFLOW__CELERY__BROKER_URL: redis://redis:6379/0
  #     AIRFLOW__CELERY__RESULT_BACKEND: db+postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
  #     AIRFLOW__CORE__FERNET_KEY: ${AIRFLOW_FERNET_KEY}
  #     AIRFLOW__CORE__LOAD_EXAMPLES: 'false'
  #   ports:
  #     - "5555:5555"
  #   command: airflow celery flower